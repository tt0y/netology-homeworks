'use strict';

const contentform = document.getElementsByClassName('contentform');
const buttons = document.getElementsByClassName('button-contact');
const output = document.getElementById('output');
elements.addEventListener('change', function (el) {
    el
});
